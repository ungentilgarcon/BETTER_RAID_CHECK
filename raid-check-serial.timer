[Unit]
Description=Scheduled RAID check with media-aware concurrency limits

[Timer]
# Default monthly run (similar to mdadm monthly check policy).
OnCalendar=monthly
RandomizedDelaySec=2h
Persistent=true

[Install]
WantedBy=timers.target
